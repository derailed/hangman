kind:       Deployment
apiVersion: extensions/v1beta1
metadata:
  name: dictionary-v1
  labels:
    app: dictionary
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app:     dictionary
        version: v1
    spec:
      imagePullSecrets:
      - name: quay-secret
      containers:
      - name:            dictionary
        image:           quay.io/derailed/dictionary:1.2
        imagePullPolicy: IfNotPresent
        args:            ["--dic", "standard"]
        resources:
          limits:
            cpu:    1.0m
            memory: 5Mi
        ports:
        - name:          http
          containerPort: 9094
